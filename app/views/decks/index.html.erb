<% @decks.find_each do |deck| %>
<%= t('deck.name')%> <%= deck.name %> <br>
<%= link_to t('link.show'),deck_path(deck) %> <br>
<%= link_to t('link.edit'),edit_deck_path(deck) %> <br>
<%= button_to t('link.destroy'), deck_path(deck), method: :delete%> <br>
<% end %>
